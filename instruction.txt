1. Open the provided .ipynb file in Google Colab.
2. Run the very first code cell to install all required dependencies (stable-baselines3, swig, gymnasium, etc.).
3. Run the remaining cells in sequential order to train both models (DQN & PPO) and generate the comparison graph.
